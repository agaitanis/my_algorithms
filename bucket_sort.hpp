#ifndef BUCKET_SORT_HPP
#define BUCKET_SORT_HPP

#include <vector>

extern void bucket_sort(std::vector<double> &a);
extern void bucket_sort2(std::vector<double> &a);

extern void test_bucket_sort();

#endif // BUCKET_SORT_HPP
